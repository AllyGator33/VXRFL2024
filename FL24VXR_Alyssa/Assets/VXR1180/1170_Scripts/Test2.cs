using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BetterName : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        int badHealth = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
